<?xml version="1.0" encoding="utf-8" ?>
<UserControl
  x:Class="Demo.UI.Settings.Views.UserControls.CardGroup"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:local="using:Demo.UI.Settings.Views.UserControls"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:viewmodels="using:Demo.UI.Settings.ViewModels.UserControls"
  d:DataContext="{d:DesignInstance Type=viewmodels:CardGroupViewModel}"
  mc:Ignorable="d">
  <!--
    StackPanel: Arranges child elements into a single line that can be oriented horizontally or vertically.
    Margin: Sets the margin around the StackPanel.
  -->
  <StackPanel Margin="0,0,0,16">
    <!--
      TextBlock: Displays text.
      Margin: Sets the margin around the TextBlock.
      Style: Applies a predefined style to the TextBlock.
      Text: Binds the text content to the Title property.
    -->
    <TextBlock
      Margin="0,0,0,12"
      Style="{StaticResource BodyStrongTextBlockStyle}"
      Text="{x:Bind Title}" />
    <!--
      StackPanel: Arranges child elements into a single line that can be oriented horizontally or vertically.
      x:Name: Sets the name of the StackPanel element.
    -->
    <StackPanel x:Name="_childrenPanel" />
  </StackPanel>
</UserControl>
